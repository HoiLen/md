# 三項演算子
```java
条件式 ? 式1 : 式2
```
`?`の左辺に`条件式`
その条件式が
* `true`なら`式1`
* `false`なら`式2`

の値を返す

つまり
```java
条件式 ? trueの値 : falseの値
```
と考えられる

ex)
```java
int a = 20;
int b = 10;
System.out.println(a > b ? "a" : "b"); //出力：a

System.out.println(a < b ? "a" : "b"); //出力：b
```